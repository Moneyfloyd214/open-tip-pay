// Cross-cutting shared types used across multiple domains
module {
  public type UserId = Principal;
  public type Timestamp = Int;
  public type CentAmount = Nat; // monetary amounts stored in cents
};
